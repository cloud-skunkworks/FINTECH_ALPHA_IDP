# IDP Platform API — Python 3.12
# Pin all versions. Run `pip-compile requirements.in` to update.

# Framework
fastapi==0.111.1
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4

# AWS
boto3==1.34.131
botocore==1.34.131

# Auth
PyJWT[crypto]==2.8.0
cryptography==42.0.8

# HTTP client (for Slack notifications)
httpx==0.27.0

# Observability
opentelemetry-sdk==1.25.0
opentelemetry-api==1.25.0
opentelemetry-instrumentation-fastapi==0.46b0
opentelemetry-instrumentation-boto3sqs==0.46b0
opentelemetry-exporter-otlp-proto-grpc==1.25.0
opentelemetry-exporter-otlp-proto-http==1.25.0
opentelemetry-instrumentation-httpx==0.46b0

# Structured logging
structlog==24.2.0

# Testing (keep in main requirements so CI can install without separate file)
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0
pytest-mock==3.14.0
moto[dynamodb,ecr,sts,codebuild]==5.0.9
